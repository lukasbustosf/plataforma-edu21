const fs = require('fs'); const path = require('path'); const entorno = process.argv[2]; if (!entorno || !['local', 'production'].includes(entorno)) { console.log('Uso: node cambiar-entorno.js [local|production]'); process.exit(1); } console.log('Cambiando a entorno: ' + entorno.toUpperCase()); const clientEnvPath = path.join(__dirname, 'client', '.env.local'); const clientConfig = entorno === 'local' ? 'NEXT_PUBLIC_API_URL=http://localhost:5000' : 'NEXT_PUBLIC_API_URL=https://plataforma-edu21-production.up.railway.app'; fs.writeFileSync(clientEnvPath, clientConfig); console.log('Cliente configurado para: ' + (entorno === 'local' ? 'localhost:5000' : 'Railway'));
